import 'package:flutter/material.dart';

class EpisodsPage extends StatelessWidget {
  const EpisodsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
